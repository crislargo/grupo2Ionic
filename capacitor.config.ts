import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appListaNgfor',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
